# 🎯 [项目名称] 本地运行守则 (Local Rules)

> [!IMPORTANT]
> **全局宪法继承**：本工作区必须绝对服从 AgentCore 的宇宙大一统宪法。在执行任何任务前，优先且强制读取下述文件：
> 👉 `/Users/icapsule/Developer/AgentCore/.antigravity/global-rules.md`

---

## 🎯 本项目专属高优军规 (Local Overrides)

由于本项目的特殊业务属性，AI 在当前工作区执行任务时，必须额外遵守以下“地方法规”。（注意：此处的规则将覆盖全局宪法的同类条款，但应当保持极度精简，仅定义本项目独有的工程架构规范）

### 1. 框架与核心架构树
- **前端规范**：[在此定义：例如强制使用 React Server Components，严禁在客户端组件越权执行 DB 逻辑]
- **包管理器**：[在此定义：例如本项目为历史遗留，特别允许使用 npm，覆盖全局禁止令]
- **数据流向**：[在此定义：例如只允许使用 Supabase 强类型联查，严禁手拼 SQL]

### 2. 本地防御与检查流水线
- **发布防御**：[在此定义：例如每次 Push 前必须跑通 Cypress E2E 测试]
- **特定偏好**：[在此定义：例如变量强制使用 PascalCase，并严禁使用任何 `console.log`]